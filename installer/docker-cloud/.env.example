# DerbyNet Cloud Deployment Environment Configuration
# Copy this file to .env and configure for your deployment
#
# cp .env.example .env
# nano .env

# =============================================================================
# MQTT Authentication
# =============================================================================

# Credentials for MQTT broker access (used by services and gateway bridge)
# Run: ./scripts/setup-mqtt-auth.sh <user> <pass> to generate the passwd file
MQTT_USER=derbynet
MQTT_PASS=changeme

# =============================================================================
# Application Settings
# =============================================================================

# Timezone (default: America/New_York)
TZ=America/New_York

# Number of racing lanes (default: 3)
LANE_COUNT=3

# Maximum file upload size (default: 16M)
MAX_UPLOAD_SIZE=16M

# Enable debug logging (default: false)
DERBY_DEBUG=false

# Cloud display mode: set to "public" when serving Pi-synced kiosk displays
# Forces first-name-last-initial for privacy. Leave empty for dev/test mode.
# DERBYNET_CLOUD_MODE=public

# =============================================================================
# Browser Virtual Hardware (cloud-only)
# =============================================================================

# Credentials served to the browser virtual-hardware pages under
# website/virtual/* via the action.virtual-mqtt-creds endpoint. Mosquitto's
# ACL restricts this user to derbynet/device/B_* publishes.
# Run setup-mqtt-auth.sh; if VIRTUAL_MQTT_PASS is unset it generates one.
VIRTUAL_MQTT_USER=virtual-device
VIRTUAL_MQTT_PASS=

# =============================================================================
# Internal service tenant override (cloud-only)
# =============================================================================

# Per-session tenant routing means every browser session is bound to a
# tenant sandbox via $_SESSION['tenant_slug']. Backend services like the
# race-server have no session, so they use a shared-secret-gated header
# pair (X-DerbyNet-Internal-Token + X-DerbyNet-Tenant) to address a specific
# tenant DB. Generate with: openssl rand -hex 32. Caddy strips both headers
# from any external request so a public client cannot spoof them.
DERBYNET_INTERNAL_TOKEN=

# Slug of the tenant sandbox the race-server should write to. Must match a
# directory under /var/lib/derbynet/tenants/. Leave empty to disable the
# override (race-server cannot reach a tenant DB).
DERBYNET_TENANT=
