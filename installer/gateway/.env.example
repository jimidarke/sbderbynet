# DerbyNet Gateway Configuration
# Copy to .env and fill in your cloud VPS details

# Cloud MQTT broker address (VPS IP or domain)
CLOUD_HOST=your-vps-ip-here

# MQTT credentials (must match cloud broker's passwd file)
CLOUD_MQTT_USER=derbynet
CLOUD_MQTT_PASS=changeme
