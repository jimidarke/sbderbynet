# DerbyNet log2ram tuning — keep /var/log in RAM to spare the SD card.
# log2ram itself is installed by _common/customize.sh.
SIZE=64M
USE_RSYNC=true
MAIL=false
PATH_DISK="/var/log"
ZL2R=false
LOG_DISK_SIZE=512M
